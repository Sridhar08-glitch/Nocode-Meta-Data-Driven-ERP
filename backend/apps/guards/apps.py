from django.apps import AppConfig


class GuardsConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.guards"
    label = "guards"
    verbose_name = "Guard Framework"
