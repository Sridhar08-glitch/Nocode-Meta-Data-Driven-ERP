from django.apps import AppConfig


class ViewsSavedConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.views_saved"
    label = "views_saved"
    verbose_name = "Views Saved"
